node_modules
dist
dist-ssr
dev-dist
coverage
*.tsbuildinfo
.DS_Store
*.local

# Environment files hold credentials and are never committed. The example file
# is the exception: it documents the keys without carrying any values.
.env
.env*
!.env.example

# Vercel CLI state, including the OIDC token it writes.
.vercel
